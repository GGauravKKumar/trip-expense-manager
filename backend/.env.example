# BusManager Python Backend - Local Development Environment
# Copy this file to .env and update the values

# PostgreSQL Connection (for local non-Docker setup)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-database-password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=busmanager

# JWT Secret (minimum 32 characters - change this!)
JWT_SECRET=your-super-secret-jwt-token-with-at-least-32-characters

# Upload directory (optional, defaults to ./uploads)
# UPLOAD_DIR=/path/to/uploads
