# BusManager Frontend - Local Development Environment
# Copy this file to .env and update the values

# ===========================================
# Python API Mode (Offline/Self-Hosted)
# ===========================================
# Set this to use the local Python backend instead of Cloud
VITE_API_URL=http://localhost:8000

# IMPORTANT: Even in Python API mode, these placeholder values are required
# to prevent bundler errors. They won't be used when VITE_API_URL is set.
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_PUBLISHABLE_KEY=placeholder-key-not-used-in-python-mode
VITE_SUPABASE_PROJECT_ID=local

# ===========================================
# Cloud Mode (Remove VITE_API_URL to use Cloud)
# ===========================================
# When using Lovable Cloud, comment out VITE_API_URL above
# and the real Supabase values will be injected automatically
